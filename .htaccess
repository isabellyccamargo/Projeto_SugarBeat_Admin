# Arquivo: .htaccess

# Liga o motor de reescrita
RewriteEngine On

# Define a pasta raiz (essencial quando em subdiretório)
RewriteBase /sugarbeat_admin/

# 1. Se o arquivo ou diretório solicitado existir, usa-o (para assets como CSS, JS, imagens)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# 2. Caso contrário, redireciona a requisição para index.php 
#    A regra está mais concisa e funciona melhor com o RewriteBase.
RewriteRule . index.php [L]