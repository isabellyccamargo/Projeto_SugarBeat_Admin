#Liga o motor de reescrita
RewriteEngine On

#Define a pasta raiz (ajuste se necessário, mas para localhost/sugarbeat deve funcionar)
RewriteBase /sugarbeat_admin/

#1. Se o arquivo ou diretório solicitado existir, usa-o (para assets como CSS, JS, imagens)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

#2. Caso contrário, redireciona a requisição para index.php e passa a URL
#O parâmetro QSA (Query String Append) garante que parâmetros GET sejam mantidos.
RewriteRule ^(.*)$ index.php [QSA,L]